<%= form_for(@micropost) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "Создать новое сообщение..." %>
  </div>
  <%= f.submit "Отправить", class: "btn btn-primary" %>
<% end %>